package user

type User struct {
	Name string
	// User struct is captialized, so it is exported and can be used outside the package

	// Name and Email is capitalized, so they are exported and can be used outside the package
	Email string 
}

